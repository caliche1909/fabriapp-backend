# ==============================================
# CONFIGURACIÓN DEL SERVIDOR
# ==============================================

# Puerto donde se ejecutará el servidor
PORT=3000

# Entorno de ejecución (development, production)
NODE_ENV=development

# ==============================================
# BASE DE DATOS POSTGRESQL
# ==============================================

# Configuración de conexión a la base de datos
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=fabriapp
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# ==============================================
# AUTENTICACIÓN JWT
# ==============================================

# Clave secreta para firmar tokens JWT (debe ser muy segura en producción)
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui_minimo_32_caracteres

# ==============================================
# CONFIGURACIÓN DE EMAIL (SMTP)
# ==============================================

# Servidor SMTP para envío de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Credenciales del email
SMTP_USER=tu_email@gmail.com
SMTP_PASSWORD=tu_password_de_aplicacion

# Email del administrador (para recibir notificaciones)
ADMIN_EMAIL=admin@tuempresa.com

# ==============================================
# FRONTEND URL
# ==============================================

# URL del frontend para enlaces en emails
FRONTEND_URL=http://localhost:5173

# ==============================================
# CLOUDINARY (Para almacenamiento de imágenes)
# ==============================================

# Configuración de Cloudinary para subida de imágenes
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# ==============================================
# CORS (Opcional)
# ==============================================

# Origen permitido para CORS (si quieres restringir)
# CORS_ORIGIN=http://localhost:5173

# ==============================================
# NOTAS IMPORTANTES:
# ==============================================

# 1. Copia este archivo como .env y rellena con tus valores reales
# 2. NUNCA subas el archivo .env a Git por seguridad
# 3. En producción, usa variables de entorno del sistema o secrets
# 4. El JWT_SECRET debe ser diferente en desarrollo y producción
# 5. Para Gmail, usa "Contraseñas de aplicación" en SMTP_PASSWORD
